# Python More Data Structures

## Project: Square Matrix Simple

This project contains a Python function that computes the square of all integers in a 2D matrix.

### Files

- `0-square_matrix_simple.py`: Contains the function `square_matrix_simple(matrix)` which returns a new matrix with each element squared.
- `0-main.py`: Test file to demonstrate the usage of `square_matrix_simple`.

### Function Prototype

```python
def square_matrix_simple(matrix=[]):
    # Returns a new matrix with each element squared
